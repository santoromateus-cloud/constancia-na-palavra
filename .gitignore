# dependências e build
node_modules/
.next/
out/
build/

# ambiente (nunca subir segredo — o repo é público)
.env
.env.local
.env*.local

# sistema
.DS_Store
*.log

# gerado antes de todo build por scripts/gerar-biblia.mjs (a Bíblia inteira, 4 MB
# que não mudam nunca) — o repo guarda a receita, não o texto
src/data/biblia/
# idem: o litoral do mapa (Natural Earth, 56 KB) sai de scripts/gerar-litoral.mjs
src/lib/litoral.ts
